# Python LSP Bundle
# Provides Python language support for code intelligence via LSP.
# The python-lsp behavior includes lsp-core which provides the tool-lsp module.

bundle:
  name: lsp-python
  version: 0.1.0
  description: Python LSP support via Pyright

# Include only the python-lsp behavior - it already includes lsp-core internally
includes:
  - bundle: lsp-python:behaviors/python-lsp.yaml
